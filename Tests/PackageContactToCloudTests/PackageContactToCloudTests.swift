import XCTest
import Contacts
@testable import PackageContactToCloud

@available(macOS 12.0, *)
final class PackageContactToCloudTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
       // XCTAssertEqual(PackageContactToCloud().allContact, [CNContact()])
    }
}
